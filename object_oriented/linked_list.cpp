#include <iostream>

// Definition of a Node in a singly linked list
struct Node
{
    int data;
    Node *next;
    Node(int data)
    {
        this->data = data;
        this->next = nullptr;
    }
};

Node *head = NULL;

int main()
{

    return 0;
}